echo "🔍 Pre-commit: lint"
npm run lint || exit 1

echo "🔍 Pre-commit: type-check"
tsc -noEmit -skipLibCheck || exit 1

echo "🔍 Pre-commit: unit tests"
npm run test:unit || exit 1

echo "✅ Pre-commit checks passed"
